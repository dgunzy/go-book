{{define "private_content"}}
<header class="private-page-heading">
  <div><p class="private-kicker">Immutable history</p><h1>Ledger</h1></div>
  <p>Transactions and account balance after each posting.</p>
</header>

<section aria-labelledby="ledger-heading">
  <h2 id="ledger-heading" class="visually-hidden">Ledger entries</h2>
  <div class="private-table-wrap">
    <table class="private-table private-table-wide private-table-stack">
      <thead><tr><th scope="col">Date</th><th scope="col">Description</th><th scope="col">Account</th><th scope="col">Type</th><th scope="col">Wager</th><th scope="col">Amount</th><th scope="col">Balance</th></tr></thead>
      <tbody>
        {{range .LedgerRows}}
        <tr><td data-label="When">{{when .OccurredAt}}</td><th scope="row">{{.Description}}</th><td data-label="Account">{{.Account}}</td><td data-label="Type"><span class="private-status">{{.TransactionType}}</span></td><td data-label="Reference">{{if .ReferenceLabel}}{{.ReferenceLabel}}{{end}}<span class="private-reference">{{.Reference}}</span></td><td class="private-money" data-label="Amount">{{money .Amount}}</td><td class="private-money" data-label="Balance">{{if .HasRunningBalance}}<strong>{{money .RunningBalance}}</strong>{{else}}&mdash;{{end}}</td></tr>
        {{else}}
        <tr><td colspan="7" class="private-empty-cell">No ledger entries yet.</td></tr>
        {{end}}
      </tbody>
    </table>
  </div>
</section>
{{end}}
